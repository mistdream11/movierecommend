doctype html
html(lang='zh-CN')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title #{title}
    link(href='https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap', rel='stylesheet')
    style(type='text/css').
      /* ========== CSS变量和基础重置 ========== */
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        -webkit-font-smoothing: antialiased;
      }

      :root {
        --bg-gradient: linear-gradient(135deg, #e0f7fa 0%, #f3e5f5 100%);
        --primary: #4361ee;
        --primary-light: #5874f5;
        --primary-dark: #3a56d4;
        --text-main: #2d3748;
        --text-secondary: #718096;
        --card-bg: #ffffff;
        --shadow: 0 4px 12px rgba(67, 97, 238, 0.12);
      }

      body {
        background: var(--bg-gradient);
        font-family: 'Inter', system-ui, -apple-system, sans-serif;
        color: var(--text-main);
        min-height: 100vh;
        padding: 40px 20px;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
      }

      h1 {
        font-size: 32px;
        font-weight: 700;
        margin-bottom: 30px;
        text-align: center;
        background: linear-gradient(135deg, var(--primary), #7b61ee);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
      }

      /* ========== 推荐电影卡片网格 ========== */
      .recommendations-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
        gap: 24px;
        margin-bottom: 40px;
      }

      .recommendation-card {
        background: var(--card-bg);
        border-radius: 12px;
        overflow: hidden;
        box-shadow: var(--shadow);
        transition: all 0.3s ease;
        display: flex;
        flex-direction: column;
        cursor: pointer;
      }

      .recommendation-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 12px 24px rgba(67, 97, 238, 0.18);
      }

      .recommendation-card img {
        width: 100%;
        height: 280px;
        object-fit: cover;
        cursor: pointer;
        transition: opacity 0.3s ease;
      }

      .recommendation-card img:hover {
        opacity: 0.85;
      }

      .card-content {
        padding: 20px;
        flex: 1;
        display: flex;
        flex-direction: column;
      }

      .movie-name {
        font-size: 16px;
        font-weight: 600;
        margin-bottom: 12px;
        color: var(--text-main);
        line-height: 1.4;
        min-height: 48px;
        display: flex;
        align-items: center;
      }

      /* ========== 操作按钮 ========== */
      .card-actions {
        display: flex;
        gap: 10px;
        margin-top: auto;
      }

      .card-btn {
        flex: 1;
        padding: 10px 12px;
        border: none;
        border-radius: 6px;
        font-size: 13px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.25s ease;
        text-align: center;
      }

      .btn-view {
        background: linear-gradient(135deg, var(--primary), var(--primary-dark));
        color: #ffffff;
      }

      .btn-view:hover {
        background: linear-gradient(135deg, var(--primary-light), var(--primary));
        box-shadow: 0 4px 12px rgba(67, 97, 238, 0.2);
        transform: translateY(-1px);
      }

      .btn-favorite {
        background: #fff5f7;
        color: #ec4899;
        border: 1.5px solid #fbcfe8;
      }

      .btn-favorite:hover {
        background: #ec4899;
        color: #ffffff;
      }

      /* ========== 页脚操作 ========== */
      .actions-footer {
        display: flex;
        justify-content: center;
        gap: 15px;
        padding: 20px;
      }

      .btn {
        padding: 12px 32px;
        border: none;
        border-radius: 8px;
        font-size: 15px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.25s ease;
      }

      .btn-primary {
        background: linear-gradient(135deg, var(--primary), var(--primary-dark));
        color: #ffffff;
        box-shadow: 0 4px 12px rgba(67, 97, 238, 0.2);
      }

      .btn-primary:hover {
        background: linear-gradient(135deg, var(--primary-light), var(--primary));
        box-shadow: 0 6px 16px rgba(67, 97, 238, 0.25);
        transform: translateY(-2px);
      }

      .btn-secondary {
        background: var(--card-bg);
        color: var(--text-main);
        border: 1.5px solid #e2e8f0;
      }

      .btn-secondary:hover {
        border-color: var(--primary);
        color: var(--primary);
      }

      /* ========== 响应式设计 ========== */
      @media (max-width: 768px) {
        h1 {
          font-size: 24px;
          margin-bottom: 20px;
        }

        .recommendations-grid {
          grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
          gap: 16px;
        }

        .recommendation-card img {
          height: 220px;
        }

        .card-content {
          padding: 15px;
        }
      }

      @media (max-width: 480px) {
        body {
          padding: 20px 10px;
        }

        .recommendations-grid {
          grid-template-columns: repeat(2, 1fr);
          gap: 12px;
        }

        .btn {
          padding: 10px 20px;
          font-size: 14px;
        }
      }

  body
    div.container
      h1 亲爱的用户 #{username}，猜你喜欢的电影:
      
      div.recommendations-grid
        -if(movieinfo && movieinfo.length > 0)
          -for(var i = 0; i < movieinfo.length; i++)
            div.recommendation-card
              img(src=movieinfo[i].picture, alt=movieinfo[i].moviename)
              div.card-content
                div.movie-name= movieinfo[i].moviename
                div.card-actions
                  button.card-btn.btn-view(type='button', onclick=`window.open('${movieinfo[i].movieurl || '#'}', '_blank')`) 查看详情
                  button.card-btn.btn-favorite(type='button', onclick=`alert('已添加到收藏: ${movieinfo[i].moviename}')`) ♥ 收藏
        -else
          p(style='text-align: center; color: var(--text-secondary); padding: 40px;') 暂无推荐电影
      
      div.actions-footer
        button.btn.btn-primary(onclick="location.href='/'") 返回首页
        button.btn.btn-secondary(onclick="location.href='/genreselect'") 继续评分
